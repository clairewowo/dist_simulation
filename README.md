# Simulate vectors (L2) with skewed distance distribution
The output is in hdf5 format matched with standard testing dataset such as Mnist fashion/digits: http://ann-benchmarks.com/fashion-mnist-784-euclidean.hdf5

## usage
```bash
cargo build --release
./target/release/dist_simulation

```